spring.application.name=discovery-server
server.port=8761

# This IS the registry, so it must not register with itself or fetch a registry
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false

# Calm the console: one replication/heartbeat per service is enough for our size
eureka.server.enable-self-preservation=false
